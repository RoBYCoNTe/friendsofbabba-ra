import ArrayOfChipField from "./ArrayOfChipField";
import StateField from "../../workflow/fields/StateField";
import { TextField, DateField, ChipField, BooleanField } from "react-admin";
const fields = {
  ArrayOfChipField,
  TextField,
  DateField,
  ChipField,
  BooleanField,
  StateField,
};

export default fields;
